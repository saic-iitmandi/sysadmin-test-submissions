# PostgreSQL Database Configuration
POSTGRES_DB=chessu
POSTGRES_USER=chessu_user
POSTGRES_PASSWORD=chessu_password

# Server Configuration
NODE_ENV=production
PORT=3001

# Database Connection (used by Node.js)
PGHOST=db
PGPORT=5432
PGDATABASE=royalchess
PGUSER=chessu_user
PGPASSWORD=chessu_password

# Session Management
SESSION_SECRET=your-super-secret-session-key-change-in-production

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Optional: For production, set to your domain
# CORS_ORIGIN=https://ches.su
