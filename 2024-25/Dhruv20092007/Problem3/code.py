# In puma.rb file I found that this app will work on its port 3000
# So when Ill create container, I have to do -p3953:3000 <app-image name>
#                                            (random)
# I build image and run it, still I got error.